<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/btn_normal"
    android:radius="6dp">//这里是扩散水波纹的色值
    <!--<item android:id="@android:id/mask" android:drawable="@color/btn_press" />里面的色值可以任选一个-->

    <item android:id="@android:id/mask" android:drawable="@drawable/bg_solid_blue" />
</ripple>
